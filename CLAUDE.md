# hospital-news-dashboard-dev (개발)

서울아산병원 홍보팀 언론Unit의 **병원 뉴스 대시보드 개발 버전**입니다.
신규 기능은 전부 여기서 만들고, 검증된 것만 운영 저장소로 옮깁니다.

- 공개 주소: https://daye8965.github.io/hospital-news-dashboard-dev/
- 운영 버전: `../hospital-news-dashboard` (별도 저장소 `hospital-news-dashboard`)

## 운영본과의 차이 (2026-09 기준)

dev에만 있는 것:

- `enrich_articles.py` — 기사 보강 (기자명·기자이메일·지면·출입기자)
- `.github/workflows/enrich.yml` — 보강 단독 실행용 (평소엔 수집 워크플로 안에서 같이 돌아감)
- 대시보드 탭 `paper`(🗞️ 지면모음), `report`(📑 서울아산병원-보고용)
- 헤더 아래 개발 버전 안내 문구

운영본 탭은 `ours`/`others`/`compare` 3개뿐입니다.

## 구조

| 파일 | 역할 |
|---|---|
| `naver_to_csv.py` | 네이버 뉴스 API 수집. 병원별 검색어, 제외 키워드/매체, 매체명 정규화, 중복 제거 |
| `enrich_articles.py` | 기사 본문·언론사 원문에서 기자명·지면 추출, 구글 시트의 출입기자 명단과 대조 |
| `clean_csv.py` | 수집 전 CSV 오염 행 정리 |
| `generate_weekly_report.py` | 주간 보고서 생성 — 매주 월요일 |
| `docs/index.html` | 대시보드 본체 (단일 파일, CSV를 클라이언트에서 읽어 렌더링) |
| `docs/news.csv` | 수집 데이터. **사람이 직접 편집하지 말 것** |
| `docs/enrich_status.json` | 보강 진행 상태 |
| `paper_manual.csv` | 지면을 손으로 지정하는 목록. 여기는 **사람이 직접 적는 파일** |

## 기능의 배경

지금의 탭 구성은 아래 요구사항에서 나왔습니다. 관련 작업을 할 때 의도를 참고하세요.

- **지면모음(`paper`)** — 온라인 기사 본문에 `A17면` 같은 지면 표기가 있는 경우가 있어, 이를 근거로 지면 기사만 따로 모읍니다.
- **보고용(`report`)** — 따로 집계하는 「주요병원 연간언론실적」 리스트와 일치하는 매체만 추려 보는 탭입니다. 대시보드가 수집하지만 실적에서 제외되는 기사(제약·지자체·타기관 행사·연예인 관련)를 걸러내려는 목적입니다.
- **출입기자 표시** — 기사 바이라인이 우리 출입기자면 옆에 표시합니다. 명단은 구글 시트(`기자리스트` 탭)에 있고 수시로 갱신되며, **크롤링 시점 기준**으로 판정합니다.

## 대시보드 화면 (`docs/index.html`)

공개 주소에서 보이는 화면 전체가 이 파일 하나입니다. 빌드 도구 없이 브라우저가 `news.csv`를
`fetch`해서 렌더링하고, CSV 로드에 실패하면 파일 안의 `SAMPLE` 19건으로 미리보기를 띄웁니다
(이때만 "샘플 데이터 미리보기" 안내가 보이고, `notice-keep` 클래스가 붙은 안내는 항상 남습니다).
외부 의존은 CDN 2개뿐 — Chart.js 4.4.1(비교 탭 차트), SheetJS xlsx 0.18.5(엑셀 다운로드).

- 문서 제목 `[개발] 빅5 병원 뉴스 대시보드`, 헤더에 `대시보드 제작 : 이다예`, 개발 버전 배지와 안내 문구.
- 헤더 우측 `총 N건` 배지 = 중복 제거 후 전체 행 수, 좌측 `마지막 수집:` = `수집일시` 최댓값.

### 탭 5개 (처음 열리는 탭은 `paper`)

| 탭 | 내용 |
|---|---|
| 🗞️ 지면모음 `paper` | 지면이 확인된 기사만. 병원 5곳 필터, 면 정렬 가능. 통계: 지면 기사·신문사 수·출입기자 기사·병원별 건수 |
| 🏥 우리병원 `ours` | `병원그룹 = 아산`. 요약 카드(전체·수집 일수·검색어별) + 매체 유형별 집계(클릭하면 기사 목록 모달) + 매체별 Top 5 |
| 🔍 타병원 `others` | 삼성·세브란스·서울대·성모. 병원 버튼으로 하나만 보기 |
| 📑 서울아산병원-보고용 `report` | 연간 언론실적 양식에 맞춘 3개 섹션 (아래 참조) |
| 📊 병원 비교 `compare` | 병원별 건수 막대·도넛 차트와 순위표. **자체 기간 필터**를 쓰므로 상단 필터바는 숨겨집니다 |

### 공통 필터바 (`compare` 탭 제외)

날짜 from/to · 키워드 select(현재 탭에 해당하는 병원 검색어만) · 검색창 · `출입기자 기사만` 체크 ·
초기화 · 결과 건수 · 엑셀 다운로드. 검색창은 **공백으로 나눈 모든 단어를 AND**로 찾고,
`-단어`는 제외입니다. 대상은 제목+요약. 한 페이지 20건(`PAGE_SIZE`), 기본 정렬은 발행일시 내림차순.

엑셀 버튼은 탭에 따라 동작이 바뀝니다 — `paper`는 지면모음 시트 1장, `report`는 섹션별 시트,
그 외는 병원별 시트로 나눈 주간 엑셀.

### 화면에서 데이터를 손보는 규칙 (모두 `index.html` 안의 상수)

- `MEDIA_MAP_FE` — URL 도메인 → 한글 매체명. **가장 긴 키가 우선**이라 `chosunbiz`·`it.chosun`이
  `chosun`보다 먼저 맞습니다. 매체명이 깨져 보이면 대개 여기에 도메인을 추가하면 됩니다.
- `classifyMedia` — 일간지/경제지/방송/통신사/의료전문지/기타 6분류.
- `isNoise` — 화면에서 지우는 기사. 검색어 `서울중앙병원`·`서울의대`는 무조건 제외,
  `FE_EXCLUDE_KW`(부고·연예인·삼성 계열사·분원 등)와 `FE_EXCLUDE_MEDIA`가 걸리면 제외.
  **판정 대상은 `NOISE_FIELDS`로 한정**합니다 — 기자명 같은 보강 열의 인명이 제외 키워드에 걸리지 않게 한 의도적 제한입니다.
- `parseCSV` 끝의 중복 제거 — 같은 URL, 또는 기호를 뗀 **제목 앞 30자**가 같으면 뒤엣것을 버립니다.
- `hasPaper` — `지면` 값이 `A17면` 같은 `^[A-Z]?\d{1,3}면$` 형태일 때만 지면 기사로 봅니다.
  `-`(`CHECKED_NONE`)는 보강이 "확인했지만 값 없음"으로 남긴 표시라 지면·기자명 모두 빈 값 취급합니다.
- `isPersonName` / `extractProfessor` — 제목·요약에서 `홍길동 교수` 패턴을 뽑는 휴리스틱(최대 3명).
  **핵심은 경계 검사**입니다 — 이름 앞에 한글이 붙어 있으면 이름으로 보지 않습니다. 이게 없으면
  `혈액종양내과 교수`에서 `양내과`, `감염내과`에서 `염내과`를 이름으로 잡습니다. 그 밖에 성씨 목록,
  일반어·직함 제외 목록, `과`/`대`로 끝나는 후보 제외로 거릅니다.
  화면은 **본문 추출만** 쓰고 CSV의 `교수명` 컬럼은 보지 않습니다 — 예전 규칙으로 모은 값이라
  `평균`·`반도체` 같은 게 섞여 있습니다.
  **같은 규칙이 `naver_to_csv.py`에도 있습니다. 한쪽만 고치지 마세요.**

### 보고용 탭의 판정 규칙

- 섹션 3개: **아산 신문**(지면 확인된 기사) / **아산 온라인** / **타병원 신문**(삼성·서울대·세브란스).
  타병원은 신문만 집계하고, 방송 프로그램 실적은 기사로 구분되지 않아 방송사 웹기사는 온라인으로 넣습니다.
- 날짜를 지정하지 않으면 **최근 7일**, 목록은 실적 양식대로 날짜 오름차순.
  머리글의 `날짜`·`매체`·`병원`을 눌러 정렬을 바꿀 수 있지만 **화면에서만** 바뀝니다
  (`sortReportItems`는 `renderReport`에서만 부릅니다). 엑셀은 실적 입력용이라 늘 날짜 오름차순입니다.
- `REPORT_ONLINE_MEDIA_NAMES` — 2026 연간 언론실적 `아산_온라인` 시트에 한 번이라도 집계된 매체.
  온라인 섹션은 이 목록에 있는 매체만 통과시킵니다.
- `REPORT_SUSPECT_RULES` — 2026-06-11~09-10 아산 기사 3,515건을 실적 엑셀과 대조해 만든
  '제외 추정' 태그. **실제 제외율 85% 이상인 유형만** 넣었고 (제약사 발표 속 코멘트 52%, 정책 발언 57% 등은
  구분이 안 돼 제외), 규칙이 아산 기사로만 검증돼서 `suspectTag`는 아산 기사에만 적용합니다.
  체크박스로 태그 달린 기사를 숨길 수 있습니다.
- 파일에 `⚠️ 임시값` 주석이 남아 있습니다 — 매체 목록·제외 규칙은 실적 엑셀이 갱신되면 같이 바꿔야 합니다.

## 데이터

`docs/news.csv` 헤더 (순서 고정):

```
날짜,병원그룹,검색어,매체,제목,교수명,요약,언론사원문,네이버링크,발행일시,수집일시,기자명,기자이메일,지면,출입기자
```

약 17,000행. 전체를 읽지 말고 `head`/`grep`/`awk`로 필요한 부분만 보세요.

### 지면 직접 추가 (`paper_manual.csv`)

지면 정보의 출처는 네이버 '신문 지면 보기' 하나뿐이라 두 경우를 자동으로 못 잡습니다.

- **특집 별지** — 네이버는 본판(A면)만 올립니다. 2026-09-16 경향신문을 확인해 보니 A1~A27뿐이고
  한가위 특집 `H04면`은 아예 없었습니다.
- **네이버 기사 링크가 없는 기사** — 전체의 약 69%입니다(전문지 대부분, 헬스경향 등).
  언론사 코드를 뽑을 수 없어 조회를 시도조차 하지 않습니다.

이럴 때 `paper_manual.csv`에 한 줄 적으면 보강이 그 값을 덮어씁니다. 사람이 적은 값이 항상 이깁니다.

```
링크,면,메모
https://www.k-health.com/news/articleView.html?idxno=101201,H4면,경향신문 2026-09-16 한가위 특집 별지
```

- `링크`는 대시보드의 기사 주소(`언론사원문`·`네이버링크` 둘 중 아무거나). 프로토콜·`www.`·
  `utm_` 추적 파라미터 차이는 무시하고 맞춥니다.
- `면`은 `A16면`·`H4면` 형태(`^[A-Z]?\d{1,3}면$`). 형식이 틀리거나 링크에 맞는 기사가 없으면
  건너뛰고 `docs/enrich_status.json`의 `직접지정지면`에 이유를 남깁니다.
- 적용은 `enrich_articles.py`의 `apply_paper_manual()`이 하고, **네이버 조회 다음에** 돕니다.

## 자동화

- 스케줄: `cron "20 19 * * *"`, `"17 23 * * *"` (UTC) — GitHub 예약 실행이 1~2시간 밀리는 것을 감안해 **앞당겨 설정**해 둔 값입니다. 시간 조정 시 이 지연을 고려하세요.
- 매주 월요일 `cron "0 0 * * 1"`에는 수집 대신 주간 보고서를 만듭니다.
- `concurrency: news-data` — 수집과 보강이 같은 CSV를 건드리므로 동시 실행을 막아 둡니다.
- 보강 단계는 `continue-on-error: true` — **보강이 실패해도 수집한 기사는 반드시 커밋**되도록 한 의도적 설계입니다.
- 커밋 충돌 시 이번 결과를 버리고 정상 종료하며, 빠진 날짜는 다음 실행의 백필이 다시 수집합니다 (`MAX_BACKFILL_DAYS = 7`).

## Secrets

`NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET`,
`GOOGLE_SERVICE_ACCOUNT_JSON`, `REPORTER_SHEET_ID`, `REPORTER_SHEET_RANGE`, `REPORTER_SHEET_CSV_URL`

## 작업 규칙

- 커밋 메시지는 기존 히스토리를 따릅니다 — 한글 서술형 또는 영문 명령형 한 줄.
- 워크플로의 `자동 수집 …` 커밋과 섞이므로 push 전에 항상 `git pull --rebase`.
- 운영 반영은 이 세션에서 하지 않습니다. dev에서 검증을 끝낸 뒤 운영 세션에서 옮기세요.
- 정적 페이지 확인: `python -m http.server 8765` 후 `http://localhost:8765/docs/`
